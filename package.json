{
  "name": "@gszabo/sample-ng1-module",
  "version": "0.1.0",
  "description": "Sample angular1 module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run compile && gulp copy-jade",
    "compile": "babel src -d dist",
    "prepublish": "rm -rf dist && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gszabo/sample-ng1-module.git"
  },
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gszabo/sample-ng1-module/issues"
  },
  "homepage": "https://github.com/gszabo/sample-ng1-module#readme",
  "devDependencies": {
    "babel-cli": "6.6.0",
    "babel-preset-es2015": "6.6.0",
    "gulp": "3.9.1"
  },
  "files": ["dist"]
}
